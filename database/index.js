const camperDatabase = require('./camper-database')
const authorDatabase = require('./author-database')
const blogPostDatabase = require('./blogpost-database')

module.export = {camperDatabase, blogPostDatabase, authorDatabase}